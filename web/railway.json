{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --legacy-peer-deps && npm run build && cp -r .next/static .next/standalone/.next/static && (cp -r public .next/standalone/public || true)"
  },
  "deploy": {
    "startCommand": "cd .next/standalone && PORT=${PORT:-3000} HOSTNAME=0.0.0.0 node server.js",
    "healthcheckPath": "/",
    "healthcheckTimeout": 120
  }
}
